describe("Data Type", function () {
    it("should must declare", function () {
        let name: string = "Raditya Putranto";
        let balance: number = 1000000;
        let isVip: boolean = true;

        console.log(name);
        console.log(balance);
        console.log(isVip);

        // error example different value with data type
        /* name = 100;
        balance = "radit";
        isVip = 100; */
    });
});
